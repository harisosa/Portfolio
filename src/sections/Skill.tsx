"use client";

import React from "react";
import { Section } from "@/components/layout/Section";

export const Skill: React.FC = () => {
  return (
    <Section id="skill">
      <div className="w-full">Skill</div>
    </Section>
  );
};
